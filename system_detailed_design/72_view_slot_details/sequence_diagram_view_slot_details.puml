@startuml view_slot_details
!theme plain
hide footbox

actor "Lecturer" as Lecturer

participant "«user interaction»\n:SlotDetailPage" as UI
participant "«control»\n:SlotController" as Controller
participant "«service»\n:SlotService" as Service
participant "«database wrapper»\n:SlotRepository" as Repository

' === MAIN FLOW ===
Lecturer -> UI: Click view slot details
activate UI

UI -> Controller: GET /api/v1/slots/{id}
activate Controller

Controller -> Service: findById(id)
activate Service

Service -> Repository: findById(id)
activate Repository
Repository -->> Service: Optional<Slot>
deactivate Repository

alt Slot not found
    Service -->> Controller: <<throw>> ResourceNotFoundException
    Controller -->> UI: 404 Not Found
    UI -->> Lecturer: Show error message
end

Service -->> Controller: SlotDTO
deactivate Service

Controller -->> UI: 200 OK
deactivate Controller

UI -->> Lecturer: Display slot details\n(time, room, staff, class info)
deactivate UI

@enduml
