@startuml delete_slot
!theme plain
hide footbox

actor "Data Operator" as DataOperator

participant "«user interaction»\n:DeleteSlotDialog" as UI
participant "«control»\n:SlotController" as Controller
participant "«service»\n:SlotService" as Service
participant "«database wrapper»\n:SlotRepository" as Repository
participant "«entity»\n:Slot" as Entity

' === MAIN FLOW ===
DataOperator -> UI: Click "Delete" on a slot
activate UI

UI -> UI: Show confirmation dialog

DataOperator -> UI: Click "Confirm"

UI -> Controller: DELETE /api/v1/slots/{id}
activate Controller

Controller -> Service: delete(id)
activate Service

Service -> Repository: findById(id)
activate Repository
Repository -->> Service: Optional<Slot>
deactivate Repository

alt Slot not found
    Service -->> Controller: <<throw>> ResourceNotFoundException
    Controller -->> UI: 404 Not Found
end

' === VALIDATION ===
Service -> Entity: getSessionStatus()
activate Entity
Entity -->> Service: status
deactivate Entity

alt Slot already started
    Service -->> Controller: <<throw>> BadRequestException
    Controller -->> UI: 400 Bad Request
end

' === CLEANUP (Internal) ===
Service -> Service: cleanupAssociatedRecords(id)
' (Omitted cascading delete of attendance records for simplicity)

' === DELETE ===
Service -> Repository: deleteById(id)
activate Repository
deactivate Repository

Service -->> Controller: void
deactivate Service

Controller -->> UI: 200 OK
deactivate Controller

UI -->> DataOperator: Close dialog\nRemove slot from list
deactivate UI

@enduml
