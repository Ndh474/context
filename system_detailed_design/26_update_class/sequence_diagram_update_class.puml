@startuml update_class
!theme plain
hide footbox

actor "Data Operator" as DataOperator

participant "«user interaction»\n:ClassFormDialog" as UI
participant "«control»\n:ClassController" as Controller
participant "«service»\n:ClassService" as Service
participant "«database wrapper»\n:ClassRepository" as Repository
participant "«entity»\n:AcademicClass" as Entity

' === MAIN FLOW ===
DataOperator -> UI: Edit class data\n(code, subjectId, semesterId, isActive)
activate UI

DataOperator -> UI: Submit form
UI -> Controller: PUT /api/v1/classes/{id}
activate Controller

Controller -> Service: update(id, request)
activate Service

' === FIND EXISTING ===
Service -> Repository: findById(id)
activate Repository
Repository -->> Service: Optional<AcademicClass>
deactivate Repository

alt Class not found
    Service -->> Controller: <<throw>> ResourceNotFoundException
    Controller -->> UI: 404 Not Found
    UI -->> DataOperator: Show error message
end

' === VALIDATION: UNIQUE CONSTRAINT ===
Service -> Repository: existsByCodeAndSubjectIdAndSemesterIdAndIdNot(code, subjectId, semesterId, id)
activate Repository
Repository -->> Service: Boolean
deactivate Repository

alt Class already exists
    Service -->> Controller: <<throw>> ResourceExistsException
    Controller -->> UI: 400 Bad Request
    UI -->> DataOperator: Show error message
end

' === LOOKUP REFERENCES (Internal) ===
Service -> Service: validateAndGetSubjectAndSemester(subjectId, semesterId)
' (Omitted interactions with other repositories for simplicity)

' === CHECK ACTIVE STATUS ===
alt Subject or Semester is inactive
    Service -->> Controller: <<throw>> OperationNotAllowedException
    Controller -->> UI: 400 Bad Request
    UI -->> DataOperator: Show error message\n"Cannot update class with inactive subject/semester"
end

' === DEACTIVATION CHECK ===
alt Deactivating class with unfinished slots
    Service -> Repository: findLastActiveSlotEndTimeByClassId(id)
    activate Repository
    Repository -->> Service: LocalDateTime
    deactivate Repository

    Service -->> Controller: <<throw>> OperationNotAllowedException
    Controller -->> UI: 403 Forbidden
    UI -->> DataOperator: Show error message\n"Cannot deactivate class that hasn't finished"
end

' === UPDATE ENTITY ===
Service -> Entity: updateFromRequest(request)
activate Entity
Entity -->> Service: academicClass
deactivate Entity

' === SAVE ===
Service -> Repository: save(academicClass)
activate Repository
Repository -->> Service: savedClass
deactivate Repository

Service -->> Controller: ClassDTO
deactivate Service

Controller -->> UI: 200 OK
deactivate Controller

UI -->> DataOperator: Show success message\nClose dialog & refresh list
deactivate UI

@enduml
