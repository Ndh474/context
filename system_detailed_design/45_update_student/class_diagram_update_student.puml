@startuml update_student
!theme plain
hide circle
skinparam classAttributeIconSize 0

' --- CẤU HÌNH "ORTHO" (BẮT BUỘC) ---
skinparam linetype ortho
skinparam nodesep 60
skinparam ranksep 60

' --- CONTROLLER ---
class StudentProfileController {
  + update(id, request) : Response
}

' --- SERVICE ---
class StudentProfileService {
  + update(id, request) : StudentProfileDTO
}

' --- REPOSITORY ---
interface StudentProfileRepository <<interface>> {
  + findById(id : Integer) : Optional<StudentProfile>
  + existsByRollNumber(rollNumber : String) : boolean
  + save(profile : StudentProfile) : StudentProfile
}

interface UserRepository <<interface>> {
  + existsByEmail(email : String) : boolean
}

interface MajorRepository <<interface>> {
  + findById(id : Short) : Optional<Major>
}

' --- ENTITIES ---
class StudentProfile {
  - userId : Integer
  - user : User
  - major : Major
  - rollNumber : String
}

class User {
  - id : Integer
  - username : String
  - email : String
  - fullName : String
  - active : Boolean
}

class Major {
  - id : Short
  - name : String
  - code : String
}

' --- DEPENDENCY INJECTION (Association) ---
StudentProfileController -down-> StudentProfileService
StudentProfileService -down-> StudentProfileRepository
StudentProfileService -down-> UserRepository
StudentProfileService -down-> MajorRepository

' --- REPOSITORY USAGE (Dependency) ---
StudentProfileRepository .down.> StudentProfile
UserRepository .down.> User
MajorRepository .down.> Major

' --- ENTITY RELATIONSHIPS (Aggregation) ---
StudentProfile o-right- User
StudentProfile o-right- Major

@enduml
