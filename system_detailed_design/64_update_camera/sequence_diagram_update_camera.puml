@startuml update_camera
!theme plain
hide footbox

actor "Data Operator" as DataOperator

participant "«user interaction»\n:CameraFormDialog" as UI
participant "«control»\n:CameraController" as Controller
participant "«service»\n:CameraService" as Service
participant "«database wrapper»\n:CameraRepository" as Repository
participant "«entity»\n:Camera" as Entity

' === MAIN FLOW ===
DataOperator -> UI: Edit camera data\n(name, rtspUrl, roomId, isActive)
activate UI

DataOperator -> UI: Submit form
UI -> Controller: PUT /api/v1/cameras/{id}
activate Controller

Controller -> Service: update(id, request)
activate Service

' === FIND EXISTING ===
Service -> Repository: findById(id)
activate Repository
Repository -->> Service: Optional<Camera>
deactivate Repository

alt Camera not found
    Service -->> Controller: <<throw>> ResourceNotFoundException
    Controller -->> UI: 404 Not Found
    UI -->> DataOperator: Show error message
end

' === VALIDATION: NAME UNIQUENESS ===
Service -> Repository: existsByNameAndIdNot(name, id)
activate Repository
Repository -->> Service: Boolean
deactivate Repository

alt name exists in another camera
    Service -->> Controller: <<throw>> ResourceExistsException
    Controller -->> UI: 409 Conflict
    UI -->> DataOperator: Show error message
end

' === VALIDATION: RTSP URL UNIQUENESS ===
Service -> Repository: existsByRtspUrlAndIdNot(rtspUrl, id)
activate Repository
Repository -->> Service: Boolean
deactivate Repository

alt rtspUrl exists in another camera
    Service -->> Controller: <<throw>> ResourceExistsException
    Controller -->> UI: 409 Conflict
    UI -->> DataOperator: Show error message
end

' === VALIDATION: ROOM EXISTS ===
Service -> Service: validateRoom(roomId)
note right: Check room existence

alt Room not found
    Service -->> Controller: <<throw>> ResourceNotFoundException
    Controller -->> UI: 404 Not Found
    UI -->> DataOperator: Show error message
end

' === UPDATE ENTITY ===
Service -> Entity: updateFromRequest(request)
activate Entity
Entity -->> Service: camera
deactivate Entity

' === SAVE ===
Service -> Repository: save(camera)
activate Repository
Repository -->> Service: savedCamera
deactivate Repository

Service -->> Controller: CameraDTO
deactivate Service

Controller -->> UI: 200 OK
deactivate Controller

UI -->> DataOperator: Show success message\nClose dialog & refresh list
deactivate UI

@enduml
