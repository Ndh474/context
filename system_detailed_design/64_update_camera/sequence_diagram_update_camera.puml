@startuml update_camera
!theme plain
hide footbox

actor Admin
participant ":CameraController" as Controller
participant ":CameraService" as Service
participant ":CameraRepository" as Repository
participant "Database" as DB

' --- MAIN FLOW ---
Admin -> Controller: PUT /api/v1/cameras/{id}
activate Controller

Controller -> Service: update(id, request)
activate Service

Service -> Repository: findById(id)
activate Repository

Repository -> DB: Query Camera by ID
activate DB
DB --> Repository: Camera Record / Null
deactivate DB

Repository --> Service: Optional<Camera>
deactivate Repository

alt Camera not found
    Service -->> Controller: <<throw>> ResourceNotFoundException
    Controller --> Admin: 404 Not Found
end

' Validation logic (internal: name/RTSP URL uniqueness, room validation)

alt Validation failed
    Service -->> Controller: <<throw>> ValidationException
    Controller --> Admin: 400 Bad Request / 404 Not Found / 409 Conflict
end

Service -> Repository: save(camera)
activate CameraRepo

CameraRepo -> DB: Update Camera
activate DB
DB --> CameraRepo: Updated Camera
deactivate DB

Repository --> Service: Camera
deactivate Repository

Service --> Controller: CameraDTO
deactivate Service

Controller --> Admin: 200 OK
deactivate Controller

@enduml
