@startuml import_csv_semester
!theme plain
hide footbox

actor "Data Operator" as DataOperator

participant "«user interaction»\n:GenericImportDialog" as UI
participant "«control»\n:SemesterController" as Controller
participant "«service»\n:SemesterService" as Service
participant "«database wrapper»\n:SemesterRepository" as Repository
participant "«entity»\n:Semester" as Entity

' === MAIN FLOW ===
DataOperator -> UI: Click "Import" button
activate UI

UI -> UI: Open import dialog

DataOperator -> UI: Select CSV file & import mode

DataOperator -> UI: Submit
UI -> Controller: POST /api/v1/semesters/import\n(file, mode)
activate Controller

alt File is empty
    Controller -->> UI: 400 Bad Request
    UI -->> DataOperator: Show error message
end

Controller -> Service: importFromCsv(file, mode)
activate Service

' === PARSE CSV ===
alt CSV parsing fails
    Service -->> Controller: <<throw>> BadRequestException
    Controller -->> UI: 400 Bad Request
    UI -->> DataOperator: Show error message
end

' === BATCH PROCESSING ===
loop For each unique CSV row
    Service -> Repository: findByCode(code)
    activate Repository
    Repository -->> Service: Semester / null
    deactivate Repository

    alt Row validation fails
        Service -> Service: Collect error, continue
    else Create new semester
        Service -> Entity: new Semester(row)
        activate Entity
        Entity -->> Service: semester
        deactivate Entity

        Service -> Repository: save(semester)
        activate Repository
        Repository -->> Service: savedSemester
        deactivate Repository
    else Update existing semester
        Service -> Entity: updateFromRow(row)
        activate Entity
        Entity -->> Service: semester
        deactivate Entity

        Service -> Repository: save(semester)
        activate Repository
        Repository -->> Service: savedSemester
        deactivate Repository
    end
end

Service -->> Controller: ImportResultDTO
deactivate Service

Controller -->> UI: 200 OK (ImportResultDTO)
deactivate Controller

UI -->> DataOperator: Show import result\n(success/failure counts)
deactivate UI

@enduml
