@startuml update_slot
!theme plain
hide footbox

actor "Data Operator" as DataOperator

participant "«user interaction»\n:SlotFormDialog" as UI
participant "«control»\n:SlotController" as Controller
participant "«service»\n:SlotService" as Service
participant "«database wrapper»\n:SlotRepository" as Repository
participant "«entity»\n:Slot" as Entity

' === MAIN FLOW ===
DataOperator -> UI: Click "Edit" on a slot
activate UI

UI -> UI: Open form with pre-filled data

DataOperator -> UI: Modify details\n(e.g., room, staff)
DataOperator -> UI: Click "Save"

UI -> Controller: PUT /api/v1/slots/{id}
activate Controller

Controller -> Service: update(id, request)
activate Service

Service -> Repository: findById(id)
activate Repository
Repository -->> Service: Optional<Slot>
deactivate Repository

alt Slot not found
    Service -->> Controller: <<throw>> ResourceNotFoundException
    Controller -->> UI: 404 Not Found
end

' === VALIDATIONS ===
Service -> Entity: getSessionStatus()
activate Entity
Entity -->> Service: status
deactivate Entity

alt Slot has ended (LIMITED EDIT)
    Service -> Service: validateActiveStatusUpdateOnly()
else Slot ongoing (VIEW ONLY)
    Service -->> Controller: <<throw>> BadRequestException
    Controller -->> UI: 400 Bad Request
end

' === CONFLICT CHECKS (If time/room changed) ===
Service -> Repository: existsByRoomAndTimeRange(...)
activate Repository
Repository -->> Service: Boolean
deactivate Repository

alt Room conflict
    Service -->> Controller: <<throw>> BadRequestException
    Controller -->> UI: 400 Bad Request
end

' === UPDATE ENTITY ===
Service -> Entity: setTitle(...)
activate Entity
deactivate Entity
Service -> Entity: setDescription(...)
activate Entity
deactivate Entity
' (Other setters omitted)

' === SAVE ===
Service -> Repository: save(slot)
activate Repository
Repository -->> Service: savedSlot
deactivate Repository

Service -->> Controller: SlotDTO
deactivate Service

Controller -->> UI: 200 OK
deactivate Controller

UI -->> DataOperator: Show success message\nRefresh list
deactivate UI

@enduml
