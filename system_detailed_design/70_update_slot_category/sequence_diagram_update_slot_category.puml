@startuml update_slot_category
!theme plain
hide footbox

actor "Lecturer" as Lecturer

participant "«user interaction»\n:LecturerSlotFormDialog" as UI
participant "«control»\n:SlotController" as Controller
participant "«service»\n:SlotService" as Service
participant "«database wrapper»\n:SlotRepository" as SlotRepo
participant "«database wrapper»\n:ExamAttendanceRepository" as ExamRepo
participant "«entity»\n:Slot" as Entity

' === MAIN FLOW ===
Lecturer -> UI: Select new slot category\n(LECTURE ↔ LECTURE_WITH_PT)
activate UI

Lecturer -> UI: Submit form
UI -> Controller: PUT /api/v1/slots/{id}/category
activate Controller

Controller -> Service: updateSlotCategory(id, request, currentUserId)
activate Service

' === FIND EXISTING ===
Service -> SlotRepo: findById(id)
activate SlotRepo
SlotRepo -->> Service: Optional<Slot>
deactivate SlotRepo

alt Slot not found
    Service -->> Controller: <<throw>> ResourceNotFoundException
    Controller -->> UI: 404 Not Found
    UI -->> Lecturer: Show error message
end

' === CHECK AUTHORIZATION ===
Service -> Service: checkAssignedStaff(slot, currentUserId)
note right: Only assigned lecturer can update

alt Not assigned to slot
    Service -->> Controller: <<throw>> OperationNotAllowedException
    Controller -->> UI: 403 Forbidden
    UI -->> Lecturer: Show error message
end

' === VALIDATE CATEGORY TRANSITION ===
Service -> Service: isValidCategoryTransition(current, new)
note right: Only LECTURE ↔ LECTURE_WITH_PT allowed

alt Invalid transition
    Service -->> Controller: <<throw>> BadRequestException
    Controller -->> UI: 400 Bad Request
    UI -->> Lecturer: Show error message
end

' === CHECK EXAM DATA (LECTURE_WITH_PT → LECTURE) ===
opt Changing from LECTURE_WITH_PT to LECTURE
    Service -> ExamRepo: countBySlotIdAndStatusNot(id, NOT_YET)
    activate ExamRepo
    ExamRepo -->> Service: Long
    deactivate ExamRepo

    alt Has PRESENT/ABSENT exam records
        Service -->> Controller: <<throw>> BadRequestException
        Controller -->> UI: 400 Bad Request
        UI -->> Lecturer: Show error message\n"Delete exam attendance first"
    end

    Service -> ExamRepo: deleteBySlotId(id)
    activate ExamRepo
    ExamRepo -->> Service: void
    deactivate ExamRepo
end

' === UPDATE ENTITY ===
Service -> Entity: setSlotCategory(newCategory)
activate Entity
Entity -->> Service: slot
deactivate Entity

' === SAVE ===
Service -> SlotRepo: save(slot)
activate SlotRepo
SlotRepo -->> Service: savedSlot
deactivate SlotRepo

Service -->> Controller: SlotDTO
deactivate Service

Controller -->> UI: 200 OK
deactivate Controller

UI -->> Lecturer: Show success message\nClose dialog & refresh
deactivate UI

@enduml
