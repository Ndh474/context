@startuml import_csv_class
!theme plain
hide footbox

actor Admin
participant ":ClassController" as Controller
participant ":ClassService" as Service
participant ":CsvParserService" as CsvParser
participant ":ClassRepository" as Repository
participant "Database" as DB

' --- MAIN FLOW ---
Admin -> Controller: POST /api/v1/classes/import
activate Controller

' File validation (internal)

alt File validation fails
    Controller --> Admin: 400 Bad Request
end

Controller -> Service: importFromCsv(file, mode)
activate Service

' --- CSV PARSING ---
Service -> CsvParser: parseClassCsv(file)
activate CsvParser

alt CSV parsing fails
    CsvParser -->> Service: <<throw>> Exception
    Service -->> Controller: <<throw>> BadRequestException
    Controller --> Admin: 400 Bad Request
end

CsvParser --> Service: List<ClassCsvRow>
deactivate CsvParser

' CSV deduplication (internal)

' --- BATCH PROCESSING LOOP ---
loop For each unique CSV row
    ' Row validation (internal: subject/semester validation, code uniqueness)

    Service -> Repository: findByCodeAndSubjectIdAndSemesterId(code, subjectId, semesterId)
    activate Repository

    Repository -> DB: Query Class by Code, Subject, Semester
    activate DB
    DB --> Repository: Class / Null
    deactivate DB

    Repository --> Service: AcademicClass
    deactivate Repository

    ' Business logic: create or update based on mode (internal)

    alt Validation fails
        Service --> Service: Collect error, continue
    else Validation passes
        Service -> Repository: save(class)
        activate Repository

        Repository -> DB: Insert or Update Class
        activate DB
        DB --> Repository: Saved Class
        deactivate DB

        Repository --> Service: AcademicClass
        deactivate Repository
    end
end

' Build result with success/failure counts (internal)

Service --> Controller: Import Result
deactivate Service

Controller --> Admin: 200 OK
deactivate Controller

@enduml
