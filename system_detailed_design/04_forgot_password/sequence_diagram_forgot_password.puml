@startuml forgot_password
!theme plain
hide footbox

actor Client
participant ":AuthenticationController" as Controller
participant ":AuthenticationService" as Service
participant ":UserRepository" as UserRepo
participant ":PasswordResetTokenRepository" as TokenRepo
participant "Database" as DB

' --- MAIN FLOW ---
Client -> Controller: POST /api/v1/auth/forgot-password
activate Controller

Controller -> Service: forgotPassword(request)
activate Service

' --- USER LOOKUP ---
Service -> UserRepo: findByEmail(email)
activate UserRepo

UserRepo -> DB: Query User by Email
activate DB
DB --> UserRepo: User Record / Null
deactivate DB

UserRepo --> Service: Optional<User>
deactivate UserRepo

' --- VALIDATION LOGIC ---
alt User not found
    Service -->> Controller: <<throw>> ResourceNotFoundException
    Controller --> Client: 404 Not Found
end

alt User is inactive
    Service -->> Controller: <<throw>> AccessDeniedException
    Controller --> Client: 403 Forbidden
end

' --- TOKEN GENERATION & STORAGE ---
group Reset Token Management
    Service -> Service: generate reset token
    Service -> Service: calculate expiry time
end

Service -> TokenRepo: updateToken(email, token, now, expiry)
activate TokenRepo

TokenRepo -> DB: Update Reset Token by Email
activate DB
DB --> TokenRepo: Affected Rows
deactivate DB

TokenRepo --> Service: Affected Rows
deactivate TokenRepo

alt Token does not exist (affected rows = 0)
    Service -> TokenRepo: save(resetToken)
    activate TokenRepo

    TokenRepo -> DB: Insert Reset Token
    activate DB
    DB --> TokenRepo: Reset Token
    deactivate DB

    TokenRepo --> Service: PasswordResetToken
    deactivate TokenRepo
end

' --- SEND EMAIL ---
group Email Notification
    Service -> Service: send password reset email with token
end

Service --> Controller: Success Message
deactivate Service

Controller --> Client: 200 OK
deactivate Controller

@enduml
