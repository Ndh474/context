@startuml create_class
!theme plain
hide footbox

actor "Data Operator" as DataOperator

participant "«user interaction»\n:ClassFormDialog" as UI
participant "«control»\n:ClassController" as Controller
participant "«service»\n:ClassService" as Service
participant "«database wrapper»\n:ClassRepository" as Repository
participant "«database wrapper»\n:SubjectRepository" as SubjectRepository
participant "«database wrapper»\n:SemesterRepository" as SemesterRepository
participant "«entity»\n:AcademicClass" as Entity

' === MAIN FLOW ===
DataOperator -> UI: Click "Create Class"
activate UI

UI -> UI: Open form dialog

DataOperator -> UI: Input class data\n(code, subjectId, semesterId)

DataOperator -> UI: Submit form
UI -> Controller: POST /api/v1/classes
activate Controller

Controller -> Service: create(request)
activate Service

' === VALIDATION: UNIQUE CONSTRAINT ===
Service -> Repository: existsByCodeAndSubjectIdAndSemesterId(code, subjectId, semesterId)
activate Repository
Repository -->> Service: Boolean
deactivate Repository

alt Class already exists
    Service -->> Controller: <<throw>> ResourceExistsException
    Controller -->> UI: 400 Bad Request
    UI -->> DataOperator: Show error message
end

' === LOOKUP SUBJECT ===
Service -> SubjectRepository: findById(subjectId)
activate SubjectRepository
SubjectRepository -->> Service: Optional<Subject>
deactivate SubjectRepository

alt Subject not found
    Service -->> Controller: <<throw>> ResourceNotFoundException
    Controller -->> UI: 404 Not Found
    UI -->> DataOperator: Show error message
end

' === LOOKUP SEMESTER ===
Service -> SemesterRepository: findById(semesterId)
activate SemesterRepository
SemesterRepository -->> Service: Optional<Semester>
deactivate SemesterRepository

alt Semester not found
    Service -->> Controller: <<throw>> ResourceNotFoundException
    Controller -->> UI: 404 Not Found
    UI -->> DataOperator: Show error message
end

' === CHECK ACTIVE STATUS ===
alt Subject or Semester is inactive
    Service -->> Controller: <<throw>> OperationNotAllowedException
    Controller -->> UI: 400 Bad Request
    UI -->> DataOperator: Show error message\n"Cannot create class with inactive subject/semester"
end

' === CREATE ENTITY ===
Service -> Entity: new AcademicClass(request)
activate Entity
Entity -->> Service: academicClass
deactivate Entity

' === SAVE ===
Service -> Repository: save(academicClass)
activate Repository
Repository -->> Service: savedClass
deactivate Repository

Service -->> Controller: ClassDTO
deactivate Service

Controller -->> UI: 201 Created
deactivate Controller

UI -->> DataOperator: Show success message\nClose dialog & refresh list
deactivate UI

@enduml
