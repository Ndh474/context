@startuml view_major_details
!theme plain
hide circle
skinparam classAttributeIconSize 0

' --- CẤU HÌNH "ORTHO" (BẮT BUỘC) ---
skinparam linetype ortho
skinparam nodesep 60
skinparam ranksep 60

' --- CONTROLLER ---
class MajorController {
  + findById(id) : Response
}

' --- SERVICE ---
class MajorService {
  + findById(id) : MajorDTO
}

' --- REPOSITORY ---
interface MajorRepository <<interface>> {
  + findMajorDTOById(id : Short) : Optional<MajorDTO>
}

' --- ENTITIES ---
class Major {
  - id : Short
  - name : String
  - code : String
  - isActive : Boolean
  - subjects : Set<Subject>
}

class Subject {
  - id : Integer
  - name : String
  - code : String
}

' --- DEPENDENCY INJECTION (Association) ---
MajorController -down-> MajorService
MajorService -down-> MajorRepository

' --- REPOSITORY USAGE (Dependency) ---
MajorRepository .down.> Major

' --- ENTITY RELATIONSHIPS (Aggregation) ---
Major o-right- Subject

@enduml
